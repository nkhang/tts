version: '3.7'
services:
    web:
        build:
            context: ./web
            dockerfile: Dockerfile.web
        image: web:latest
        ports:
            - "3000:3000"
    worker:
        build:
            context: ./worker
            dockerfile: Dockerfile.worker
        image: worker:latest
        ports:
            - "3001:3001"
